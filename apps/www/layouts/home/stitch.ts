import { styled } from 'ui';

const Hero = styled('section', {
  alignItems: 'center',
  display: 'flex',
  flexDirection: 'column',
  fontWeight: '$500',
  gap: '$4',
  width: '$full',
  zIndex: '0',
});

export const HomeStitch = {
  Hero,
};
